# Randomizing-Passcode
This is an Embedded Systems project with the end goal of operating a lock where the passcode, using an 8-bit shift register, is only known by the user of the lock, since in the code, each Q pin on the shift register will have a dedicated value/operator that only the user knows. It's also random each time, and you only get three tries, or the code shuts down.  
